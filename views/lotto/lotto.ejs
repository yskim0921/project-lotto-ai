<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>로또번호 분석 페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <!-- W3.CSS 적용 -->
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/5/w3.css">
    <link rel="stylesheet" href="public\css\style.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f8f9fa;
            padding: 20px;
        }

        .container {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            max-width: 600px;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 24px;
        }

        .input-group {
            margin-bottom: 15px;
        }

        .input-group label {
            width: 100px;
            display: inline-block;
        }

        .input-group input {
            width: calc(100% - 110px);
            padding: 6px 8px;
            border: 1px solid #ced4da;
            border-radius: 4px;
        }

        button {
            width: 100%;
            padding: 10px;
            border: none;
            background: #4caf50;
            color: #fff;
            border-radius: 6px;
            font-size: 14px;
            cursor: pointer;
        }

        button:hover {
            background: #45a049;
        }

        .loading {
            display: none;
            text-align: center;
            margin: 15px 0;
            color: #555;
        }

        .result-section {
            margin-top: 20px;
            display: none;
        }

        .result-box {
            background: #f1f3f5;
            padding: 15px;
            border-radius: 6px;
            border-left: 4px solid #4caf50;
        }

        .error {
            background: #ffe3e3;
            color: #c62828;
            padding: 10px;
            border-radius: 6px;
        }

        .back-link {
            display: inline-block;
            margin-bottom: 15px;
            color: #4caf50;
            text-decoration: none;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <!-- 상단 include -->
    <%- include('../ins/top') %>

    <div class="container">
        <a href="/" class="back-link">← 이전으로 돌아가기</a>
        <h1>로또번호 분석 페이지</h1>

        <div class="input-group">
            <label for="gender">성별:</label>
            <input type="text" id="gender" value="남성" />
        </div>

        <div class="input-group">
            <label for="calendar">달력:</label>
            <input type="text" id="calendar" value="양력" />
        </div>

        <div class="input-group">
            <label for="year">출생년도:</label>
            <input type="number" id="year" value="1990" />
        </div>

        <div class="input-group">
            <label for="month">출생월:</label>
            <input type="number" id="month" value="3" />
        </div>

        <div class="input-group">
            <label for="day">출생일:</label>
            <input type="number" id="day" value="15" />
        </div>

        <div class="input-group">
            <label for="dream">오늘의 꿈:</label>
            <input type="text" id="dream" value="돼지가 우리집으로 날아들어왔어요" />
        </div>

        <button onclick="sendData()">운세 & 로또번호 분석하기</button>

        <div class="loading" id="loading">분석 중입니다...</div>

        <div id="resultSection" class="result-section">
            <h3>분석 결과</h3>
            <div id="results" class="result-box"></div>
        </div>

        <div id="errorsSection" class="result-section">
            <div id="errors" class="error"></div>
        </div>
    </div>

    <!-- bottom include -->
    <%- include('../ins/bottom') %>

    <script>
        function sendData() {
            const gender = document.getElementById("gender").value.trim();
            const calendar = document.getElementById("calendar").value.trim();
            const year = document.getElementById("year").value.trim();
            const month = document.getElementById("month").value.trim();
            const day = document.getElementById("day").value.trim();
            const dream = document.getElementById("dream").value.trim();

            document.getElementById("loading").style.display = "block";
            document.getElementById("resultSection").style.display = "none";
            document.getElementById("errorsSection").style.display = "none";
            document.getElementById("errors").innerHTML = "";

            fetch("/lotto/calc", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ gender, calendar, year, month, day, dream })
            })
                .then(res => res.json())
                .then(data => {
                    document.getElementById("loading").style.display = "none";
                    if (data.error) {
                        displayError("서버 오류: " + data.error);
                        return;
                    }
                    displayResults(data);
                })
                .catch(err => {
                    document.getElementById("loading").style.display = "none";
                    displayError("네트워크 오류가 발생했습니다.");
                });
        }

        function displayResults(data) {
            const resultBox = document.getElementById("results");
            resultBox.innerHTML = `
                <p><strong>성별:</strong> ${data.gender}</p>
                <p><strong>달력:</strong> ${data.calendar}</p>
                <p><strong>생년월일:</strong> ${data.year}-${data.month}-${data.day}</p>
                <p><strong>꿈:</strong> ${data.dream}</p>
                <hr>
                <p>${data.message || ''}</p>
            `;
            document.getElementById("resultSection").style.display = "block";
        }

        function displayError(msg) {
            document.getElementById("errors").innerText = msg;
            document.getElementById("errorsSection").style.display = "block";
        }
    </script>
</body>

</html>
