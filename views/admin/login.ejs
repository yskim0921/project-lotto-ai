<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>관리자 로그인 - AI 로또 분석</title>

  <!-- 공통 CSS: 모든 페이지에 적용될 CSS -->
  <link rel="stylesheet" href="/css/reset.css"> <!-- 브라우저 기본 스타일 초기화 -->
  <link rel="stylesheet" href="/css/style.css"> <!-- 공통 레이아웃 및 스타일 -->

  <!-- 전용 CSS: login 전용 스타일 -->
  <link rel="stylesheet" href="/css/login.css"> <!-- 필요 시 별도 스타일 적용 -->

</head>
<body>

  <div class="container right-panel-active">
    <!-- ===========================
         로그인 폼 (login In)
         =========================== -->
    <div class="container__form container--signin">
      <form id="form2" class="form"
            action="/admin/login" method="POST">
        <!-- action, method 추가: 서버 라우터와 연결 -->
        <h2 class="form__title">관리자 로그인</h2>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message" style="background: #ffebee; color: #c62828; padding: 10px; border-radius: 4px; margin-bottom: 20px; border-left: 4px solid #f44336;">
                <%= error %>
            </div>
        <% } %>
        
        <!-- 로그인 시 사용자명 사용 -->
        <input type="text" name="username" placeholder="관리자 ID (admin)" class="input" required />
        <!-- 로그인 시 패스워드 사용 -->
        <input type="password" name="password" placeholder="Password (1234)" class="input" required />
        <!-- 메인 페이지로 돌아가기 링크 -->
        <a href="/" class="link">메인 페이지로 돌아가기</a>
        <button type="submit" class="btn">로그인</button>
      </form>
    </div>

    <!-- ===========================
         Overlay (좌우 전환 버튼)
         =========================== -->
    <div class="container__overlay">
      <div class="overlay">
        <!-- 왼쪽 패널: 로그인 폼 보여주기 -->
        <div class="overlay__panel overlay--right">
          <button class="btn" id="signIn">관리자 로그인</button>
        </div>
      </div>
    </div>
  </div>

  <!-- JavaScript 파일 연결 -->
  <script src="/js/signin_up.js"></script> 
  <!-- signin_up.js: 버튼 클릭 시 Sign In/Sign Up 폼 전환 기능 -->
</body>
</html>
