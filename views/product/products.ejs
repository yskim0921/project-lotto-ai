<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <title>상품 목록</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- W3.CSS 적용 -->
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/5/w3.css">
  <link rel="stylesheet" href="public\css\style.css">
</head>

<body>
  <!-- 상단 include -->
  <%- include('../ins/top') %>

    <div class="container w-75 mt-5 mx-auto">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="m-0">후기 목록</h2>
        <div>
          <!-- 상품 등록 버튼 -->
          <a class="btn btn-outline-secondary me-2" href="/product/upload">후기 등록</a>
        </div>
      </div>

      <% if (!products || products.length===0) { %>
        <div class="alert alert-info">등록된 후기내용이 없습니다. 먼저 등록해보세요.</div>
        <% } else { %>
          <div class="row row-cols-1 row-cols-md-2 g-4">
            <% products.forEach(p=> { %>
              <div class="col">
                <div class="card h-100">
                  <% if (p.filename) { %>
                    <a href="/product/<%= p._id %>">
                      <img src="/upload/<%= p.filename %>" class="card-img-top" alt="상품 이미지"
                        style="height:200px; object-fit:cover;">
                    </a>
                    <% } %>
                      <div class="card-body">
                        <h5 class="card-title"><b>
                            <%= p.name %>
                          </b></h5>
                        <p class="card-text text-muted mb-1">₩ <%= p.price.toLocaleString() %>
                        </p>
                        <p class="card-text">
                          <%= p.content %>
                        </p>
                      </div>
                      <div class="card-footer d-flex justify-content-between align-items-center">
                        <small class="text-muted">
                          <%= new Date(p.createdAt).toLocaleString() %>
                        </small>
                        <!-- 상세보기 링크 -->
                        <a class="btn btn-sm btn-primary" href="/product/<%= p._id %>">상세보기</a>
                      </div>
                </div>
              </div>
              <% }) %>
          </div>
          <% } %>
    </div>

    <!-- bottom include -->
    <%- include('../ins/bottom') %>
</body>

</html>