<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>감정 숫자 분류표</title>
    <link rel="stylesheet" href="/css/mind_num_mat.css" />
</head>
<body>
    <%- include('../ins/top') %>

    <div class="main-content">
        <h1>감정 숫자 분류표</h1>
        <div id="emotion-container">
            <% matching_emotion_number.forEach(emotion => { %>
                <div class="emotion-box">
                    <span class="emotion-title"><%= emotion.감정 %></span>
                    <div class="number-info">
                        <% emotion.데이터.forEach(item => { %>
                            <p class="number-item"><strong><%= item.숫자 %></strong> : <%= item.상징 %></p>
                        <% }) %>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    
    <%- include('../ins/footer') %>

    <script src="/js/mind_num_mat.js"></script>
</body>
</html>